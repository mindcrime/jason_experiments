started.

oracle_killed.


+started <- .print( "Checking if Oracle is killed" ); 
    .print( "Oracle is killed: ", .count(oracle_killed) );
    +oracle_killed;
    .print( "Oracle is killed: ", .count(oracle_killed) );
    +oracle_killed;
    .print( "Oracle is killed: ", .count(oracle_killed) );
    -oracle_killed;
    .print( "Oracle is killed: ", .count(oracle_killed) ).