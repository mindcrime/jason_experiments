started.

+started <- .print( "A. Smith. Agent Smith." ); .send( neo, askOne, zion_access_codes ).

+fuck_off[source(neo)] <- .print( "Mr. Anderson. That is the sound of your demise." ).

+fuck_off[source(random_dude)] <- .print( "Go away dude." ).
